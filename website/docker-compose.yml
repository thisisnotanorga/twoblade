services:
  app:
    build:
      context: ..
      target: production-main
      dockerfile: Dockerfile
    ports:
      - "3002:3000"
    env_file:
      - .env
    depends_on:
      - websocket

  websocket:
    build:
      context: ..
      target: production-websocket
      dockerfile: Dockerfile
    ports:
      - "8081:8080"
    env_file:
      - .env